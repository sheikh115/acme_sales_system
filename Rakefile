# frozen_string_literal: true

require 'rake/testtask'

# Define the test task to run all tests
Rake::TestTask.new do |t|
  t.libs << 'lib' # Include the 'lib' directory to load your project files
  t.pattern = 'test/test_*.rb' # Match all test files in the 'test' directory
end
